{
  "name": "@openclaw/clawtrack",
  "version": "1.0.0",
  "description": "ClawTrack integration plugin for OpenClaw",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "openclaw": {
    "extensions": [
      "./src/index.ts"
    ]
  },
  "dependencies": {
    "openclaw": "*"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "@vitest/coverage-v8": "^4.1.2",
    "vitest": "^4.1.2"
  }
}
